import pandas as pd # import once at the top of your script

# Sort values of x in descending order
# To sort in ascending order: change False into True
df = df.sort_values(by=['x'], ascending=False)
